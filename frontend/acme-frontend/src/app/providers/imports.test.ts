import { describe, it } from 'vitest'
import './QueryClient'
import './Theme'

describe('providers import', () => {
  it('imports providers without throwing', () => {
    // import side-effects are enough
  })
})
